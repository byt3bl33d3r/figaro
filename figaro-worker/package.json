{
  "name": "figaro-worker-bun",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "mkdir -p dist && bun build src/index.ts --compile --outfile dist/figaro-worker",
    "test": "bun test",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "nats": "^2.29.0",
    "@anthropic-ai/claude-agent-sdk": ">=0.2.62",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  }
}
